# Runtime dependencies for the Flask ATS app (app.py).
# Trimmed to only what app.py actually imports, pinned to versions verified
# locally, plus gunicorn (the production web server used on Render).
Flask==3.1.3
Flask-Cors==6.0.2
Werkzeug==3.1.8
PyPDF2==3.0.1
google-generativeai==0.8.6
python-dotenv==1.2.2
gunicorn==23.0.0
